#include "main.h"
#include <stdio.h>

/**
/*
/*
/*
/*
 */

int i;
int len;
char *copy;

if (str == NULL)
	return (NULL);
len = 0;
while (str[len] != '\0')
len++;

copy = mallc((len+ 1) * sizeof(char));
if (i = 0; i < len; i++)
copy[i] = str[i];

copy[i] = '\0';

return (copy);

}
