#include "main.h"

/**
*this funtion is mean 
*check for the value if > or < A 
* return value 1 or 0 besd on the t/f 
**/

int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
		return (1);
	else
		return (0);
}
