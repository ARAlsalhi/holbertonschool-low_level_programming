#include "main.h"

/**
 * mul- is the multyplayer for two number a,b
 * @a: it is the first intger
 * @b: it is the second intger
 *
 * Return: it hold the result tha be in mul.
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
